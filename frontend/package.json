{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && node esbuild.mjs",
    "dev": "node esbuild.mjs --dev --watch",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@preact/signals": "^2.0.0",
    "preact": "^10.25.0"
  }
}
